{
  "name": "callbaks",
  "version": "1.0.0",
  "description": "Use callback functions to handle the results of asynchronous operations",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.5.0"
  }
}
